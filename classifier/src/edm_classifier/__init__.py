"""EDM subgenre classifier — audio feature extraction, model and inference API."""

from edm_classifier.config import NUM_CLASSES, SUBGENRES, settings

__all__ = ["SUBGENRES", "NUM_CLASSES", "settings"]

__version__ = "0.1.0"
